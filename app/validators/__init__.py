"""
Validators package.

Contains reusable input-validation utilities used by the CLI/handlers
layer before data is passed on to the services layer (e.g. validating
required fields, date formats, allowed status values, string lengths).
"""
